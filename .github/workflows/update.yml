name: Continuous Integration
on: push
jobs:
  update_package_names:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Set Up Python 
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Install package
        run: |
          pip install beautifulsoup4==4.12.0
          pip install requests==2.28.2
      - name: Run update script 
        run : |
         python update_package_names.py
      - name: Git Commit 
        run: |
          git config --global user.name "jeffrey82221"
          git config --global user.email "jeffrey82221@gmail.com"
          git add .
          git diff-index --quiet HEAD || git commit -m 'update: package names'
      - name: Git Push
        uses: ad-m/github-push-action@master
        with:
          github_token: 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
      